@model IEnumerable<IndianArmyWeb.Areas.Admin.View_Models.MenuUrlMasterIndxVM>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UrlPrefix)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Controller)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Action)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UrlPrefix)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Controller)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Action)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MenuUrlId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MenuUrlId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MenuUrlId })
        </td>
    </tr>
}

</table>
